# OAI 주간 보고 - 2026-08-06

> 보고 기간: 2026-08-03 (월) ~ 2026-08-06 (목)
>
> 기준 브랜치: `codex/core-stabilization` (원격 `origin/codex/core-stabilization` 추적)
>
> 작성 기준: Git 이력 및 작업 트리, PM2 상태, 애플리케이션 로그, SQLite 사용자 데이터, 전체 서비스 점검 결과

## 이번 주 진행 상황

### 1. 다국어 사용자 경험 확장

- `3c555f4` (`feat(i18n): comprehensive multilingual support across all user-facing pages`) 커밋으로 사용자 대면 페이지 전반의 다국어 지원을 확장했습니다.
- 정적 번역 언어는 한국어, 영어, 일본어, 중국어, 베트남어, 네팔어이며, 인도네시아어·몽골어·라오어는 Google Website Translate 기반 동적 번역을 사용합니다.
- 한국어 학습 문장, 자막, 대화 등 연습 콘텐츠가 자동 번역으로 변형되지 않도록 `notranslate` 및 `translate="no"` 적용 원칙을 정리했습니다.
- i18n 구조와 신규 문구 추가 시 모든 정적 로케일 파일을 함께 갱신해야 하는 규칙을 `AGENTS.md`에 반영했습니다.
- 점검 결과, 일부 동적 UI는 언어 전환 직후 다시 렌더링되지 않고, 문서 언어 속성도 갱신되지 않는 보완 사항이 확인됐습니다. 이 항목은 다음 주 접근성 및 i18n 보완 작업으로 이관합니다.

### 2. 음성통화 및 TTS 안정화

- `ff893bb` (`fix(voice-call): restore mic detection pre-check and add i18n keys`) 커밋으로 음성통화 시작 전 마이크 감지 절차를 복구하고 관련 다국어 문구를 추가했습니다.
- `87f1d6b` (`fix(tts): wire up Gemini TTS service helpers in app state`) 커밋으로 Gemini TTS 호출, 캐시, PCM/WAV 변환 헬퍼를 FastAPI `app.state`에 연결했습니다.
- 8월 3일-4일 로그에서 Gemini TTS `400 INVALID_ARGUMENT` 오류 86건을 확인했습니다. 음성이 없는 요청이 유효한 Gemini 음성으로 정규화되지 않는 경로가 원인이었습니다.
- 현재 작업 트리의 `backend/routes/tts.py`에는 음성 미지정 요청이 `.env`의 `GEMINI_TTS_VOICE` 값(현재 `Leda`)을 사용하도록 하는 후속 수정이 포함돼 있습니다.
- 수정 후 Gemini TTS 음성 후보 로직을 직접 확인했으며, 전체 Python 테스트도 통과했습니다. 단, 이 후속 수정은 아직 커밋 및 운영 배포 확인 전입니다.
- 최신 활성 로그에서는 `ERROR`, traceback, 5xx가 발견되지 않았고, TTS 생성 및 Gemini Live 음성통화 연결·대화·종료 흐름이 정상적으로 기록됐습니다.

### 3. 사용자·운영 현황 점검

- SQLite 사용자 데이터 기준으로 이번 주 신규 가입은 총 6명입니다.
- 8월 3일 신규 가입은 5명, 8월 4일 신규 가입은 1명입니다.
- 8월 1일-5일 누적 성공 로그인은 34건, 신규 가입은 8명입니다.
- 로그인 이후 대시보드, SpeechPro 발음 연습, OnuiTube, 역할극, 음성통화 등 주요 학습 흐름의 이용 기록을 확인했습니다.
- `onui-ai` PM2 프로세스는 점검 시 online 상태였습니다.
- 프로세스 가동 시간은 약 45시간, 재시작 횟수는 0회, 메모리 사용량은 약 216 MB였습니다.
- PM2 CLI 버전은 7.0.3, 실행 중 데몬 버전은 6.0.14로 서로 달랐습니다. PM2가 `pm2 update`를 권고하는 상태이나, 서비스 재기동이 수반될 수 있으므로 별도 배포 창에서 수행해야 합니다.
- PM2 로그 회전 모듈은 online 상태입니다. 다만 애플리케이션별 보존 기간·최대 파일 크기·개인정보 포함 여부는 별도 점검이 필요합니다.

### 4. 전체 서비스 보안 및 기능 점검

- 전체 서비스 점검 결과는 `SERVICE_AUDIT_2026-08-05.md`에 별도 저장했습니다.
- 로그인 로그아웃은 서버에서 세션을 제거하지만 서명된 토큰 자체는 만료 전 다시 활성화될 수 있어, 로그아웃이 실질적인 토큰 폐기로 작동하지 않는 문제를 확인했습니다.
- `/api/stt/proxy`는 요청자가 외부 호출 대상 URL을 지정할 수 있어 내부망 또는 localhost를 향한 SSRF 위험이 있습니다.
- SpeechPro 설정 변경의 관리자 권한 검증이 실제 앱 상태에 강제 연결되지 않은 경로를 확인했습니다.
- SpeechPro 평가 음성은 서버 저장 후 정적 경로로 제공될 수 있어 사용자 음성 파일 공개 위험이 있습니다.
- 일반 로그인 사용자도 OnuiTube 공용 카탈로그를 쓰기 API로 변경할 수 있는 권한 문제를 확인했습니다.
- 음성통화 번역 API는 인증, 크레딧, 요청 제한 없이 Gemini를 호출할 수 있어 비용 및 가용성 악용 가능성이 있습니다.
- 대시보드, SpeechPro, 역할극, Onui Beats에서 사용자 입력·AI 응답·카탈로그 데이터를 `innerHTML`로 렌더링하는 XSS 위험을 확인했습니다.
- Google OAuth는 OAuth state를 저장할 `SessionMiddleware`가 없어 로그인 시작 요청에서 500 오류가 발생하는 상태입니다.
- 로그인 토큰을 응답 본문으로 반환하고 브라우저 `localStorage`에 저장하는 구조라, XSS 발생 시 토큰 탈취 위험이 커집니다.
- STT, SpeechPro, FluencyPro 오디오 업로드 경로는 파일 크기, 오디오 길이, MIME 타입, 처리 시간의 제한이 부족합니다.
- TTS와 이미지 생성은 입력·공급자 호출 완료 전에 크레딧을 차감하므로, 잘못된 요청이나 외부 API 실패 시 사용자가 크레딧을 잃을 수 있습니다.
- 애플리케이션 로그에는 로그인 이메일과 음성통화 전사 내용이 포함됩니다. 개인정보 마스킹 및 보존 기간 정책이 필요합니다.

### 5. 개발 환경 및 품질 검증

- `.venv/bin/python -m pytest`를 실행했고, 전체 12개 테스트가 통과했습니다.
- 현재 테스트는 설정, 인증 보안 일부, 데이터베이스, DALL-E 서비스, OnuiTube 카탈로그 중심의 Python 단위 테스트입니다.
- OAuth, 세션 폐기, 권한 경계, SSRF 차단, 오디오 업로드 제한, 공급자 실패 시 크레딧 보상에 대한 통합 테스트는 없습니다.
- 프론트엔드에는 XSS 회귀, 브라우저 기능, 접근성, 모달 포커스, 키보드 탐색, 모든 로케일 키 완결성을 검증하는 자동 테스트가 없습니다.
- `AGENTS.md`를 실제 실행 명령과 현재 구조에 맞게 정리했습니다. 특히 bare `pytest`가 `backend` import 문제를 낼 수 있어 `.venv/bin/python -m pytest` 사용이 필요하다는 점을 명시했습니다.

### 6. 현재 Git 작업 트리 상태

- 작업 트리는 clean 상태가 아닙니다. 기존 변경은 되돌리거나 수정하지 않았습니다.
- 수정된 추적 파일은 `AGENTS.md`, `backend/routes/tts.py`, `data/landing_intent.json`입니다.
- `data/landing_intent.json`에는 랜딩 페이지 가입 의도 이벤트 13건이 추가돼 있습니다. 애플리케이션 운영 데이터인지 릴리스에 포함할 시드 데이터인지 결정이 필요합니다.
- `SERVICE_AUDIT_2026-08-05.md`는 이번 서비스 점검 결과 문서입니다.
- `docs/weekly/`에는 기존 주간 보고서와 이번 보고서가 있습니다.
- `.codex/`, `.playwright-mcp/`, `QWEN.md`, `home/`은 로컬 에이전트 또는 작업 환경 산출물로 보이며, 저장소에 포함할 필요가 있는지 확인해야 합니다.
- `data/users.db-wal`, `data/users.db-shm`은 SQLite WAL 실행 중 생성 파일입니다. 일반적으로 커밋 대상이 아닙니다.
- `onui-top.png`, `onui-translate-bar.png`, `onui-translate-bar2.png`는 이미지 산출물입니다. 디자인 변경에 필요한지 확인한 뒤 별도 커밋으로 분리해야 합니다.

## 다음 주 진행 예정 작업

### 1주차: 긴급 보안 차단과 권한 경계 복구 (2026-08-10 ~ 2026-08-14)

1. 세션 토큰 폐기 구조를 수정합니다.
   - 로그아웃한 토큰이 다시 활성화되지 않도록 서버 저장 세션 또는 폐기 목록을 도입합니다.
   - 로그아웃 후 기존 cookie 및 bearer token이 모두 401을 반환하는 통합 테스트를 추가합니다.
   - 토큰을 JSON 응답 및 `localStorage`에 저장하는 구조를 제거하고 Secure·HttpOnly 쿠키 기반으로 전환할 범위를 확정합니다.

2. STT 프록시 SSRF를 차단합니다.
   - 사용자 제공 `base_url`, `endpoint` 입력을 제거하거나 엄격한 allowlist로 제한합니다.
   - loopback, 사설 IP, 링크-로컬 주소, 클라우드 메타데이터 주소, 리다이렉트를 차단합니다.
   - 허용·차단 URL에 대한 자동 테스트를 추가합니다.

3. SpeechPro 접근을 보호합니다.
   - SpeechPro 대상 URL·모델 설정 변경에 `get_current_admin_user` 의존성을 강제합니다.
   - 업로드 음성을 정적 `/uploads`에서 분리하고, 소유자 또는 관리자만 조회하는 인증 API로 전환합니다.
   - 기존 공개 음성 파일의 보존·삭제·이관 방안을 결정합니다.

4. 공용 콘텐츠 쓰기 권한을 수정합니다.
   - `POST /api/tube/videos`를 관리자 전용으로 전환합니다.
   - 카탈로그 JSON의 스키마 검증, 원자적 저장, 실패 시 복구, 백업을 추가합니다.

5. **완료 기준**: 일반 사용자·비로그인 사용자가 관리자 설정, 공용 카탈로그, 타 사용자 음성에 접근 또는 수정하지 못하며, 내부망을 향한 STT 요청이 모두 거부됩니다.

### 2주차: XSS 제거와 인증·OAuth 정상화 (2026-08-17 ~ 2026-08-21)

1. `innerHTML` 안전성 문제를 제거합니다.
   - 대시보드 최근 발음 카드, SpeechPro 음절·음소 결과와 AI 피드백, 역할극 AI 피드백, Onui Beats 카탈로그·가사·정답·뜻을 우선 수정합니다.
   - 텍스트는 `textContent`, 속성은 DOM property 또는 `setAttribute`로 설정합니다.
   - HTML이 꼭 필요한 곳만 allowlist sanitizer를 적용합니다.
   - 악성 HTML, 이벤트 속성, JavaScript URL을 이용한 XSS 회귀 테스트를 추가합니다.

2. Google OAuth를 복구합니다.
   - `SessionMiddleware`를 추가해 Authlib OAuth state를 저장합니다.
   - 로그인 시작, 콜백 성공, 콜백 실패, 취소 흐름을 통합 테스트합니다.

3. 인증 보호를 보완합니다.
   - 비밀번호 로그인에 계정 및 IP 단위 rate limit 또는 점진적 지연을 적용합니다.
   - 인증 실패 로그에서 이메일, 토큰, 내부 예외가 노출되지 않도록 조정합니다.
   - CORS, cookie `Secure`/`SameSite`, 운영 도메인 설정을 Nginx 구성과 함께 검토합니다.

4. **완료 기준**: 사용자·AI·카탈로그 응답의 악성 문자열이 실행되지 않고, Google OAuth 로그인과 일반 로그인 실패 제한이 정상 동작합니다.

### 3주차: AI 비용 통제와 미디어 처리 안정화 (2026-08-24 ~ 2026-08-28)

1. AI 비용 보호를 적용합니다.
   - `/api/voice-call/translate`에 로그인 검증, 사용자별 크레딧, IP·계정별 요청 제한, 입력 길이 제한, timeout을 추가합니다.
   - AI 공급자 오류는 안전한 사용자 메시지로 변환하고 원본 예외·입력값은 로그에 노출하지 않도록 조정합니다.

2. STT, SpeechPro, FluencyPro 업로드를 제한합니다.
   - 최대 파일 크기, MIME 타입, 확장자, 오디오 길이, ffmpeg·외부 API timeout을 적용합니다.
   - 사용자별 동시 처리 수와 시간당 요청 한도를 설정합니다.
   - 대용량 파일을 메모리에 모두 읽는 구현을 스트리밍 또는 임시 파일 기반으로 전환할지 결정합니다.

3. 크레딧 처리 순서를 안정화합니다.
   - TTS·이미지 생성은 요청 형식·입력 길이·필수 필드를 검증한 뒤 크레딧을 차감합니다.
   - 외부 공급자 실패 시 크레딧을 복원하거나, 성공 시점에 확정하는 처리 방식을 구현합니다.
   - 성공, 입력 오류, 공급자 오류, timeout별 크레딧 잔액 회귀 테스트를 추가합니다.

4. TTS 변경을 운영 반영합니다.
   - `backend/routes/tts.py`의 기본 Gemini 음성 fallback 변경을 독립 커밋으로 분리합니다.
   - 음성 미지정, 여성·남성 별칭, 유효하지 않은 음성, Gemini 실패 후 MzTTS fallback을 테스트합니다.
   - PM2 재시작 후 `.env`의 `GEMINI_TTS_VOICE=Leda` 적용 여부를 API와 로그로 확인합니다.

5. **완료 기준**: 무인증 AI 비용 소모가 차단되고, 대용량·비정상 오디오 요청이 안전하게 거부되며, TTS 실패에서 사용자의 크레딧이 보존됩니다.

### 4주차: i18n·접근성·운영 위생 및 배포 검증 (2026-08-31 ~ 2026-09-04)

1. i18n과 접근성을 보완합니다.
   - 언어 전환 시 `document.documentElement.lang`을 갱신합니다.
   - 대시보드, 영상 학습, SpeechPro, 역할극의 동적 UI가 번역 갱신 이벤트에서 다시 렌더링되도록 통일합니다.
   - `dash.view_full`, `adm.current_mode` 등 누락 키를 6개 정적 로케일에 추가하고 키 완결성 검사를 자동화합니다.
   - 클릭 가능한 랜딩 페이지 `div`를 `<a>` 또는 `<button>`으로 교체합니다.
   - 모달에 `role="dialog"`, `aria-modal`, 접근 가능한 닫기 이름, 포커스 이동·고정, Escape 닫기를 적용합니다.

2. 로그와 배포 환경을 정리합니다.
   - 이메일, IP, 음성통화 전사문을 마스킹하고 로그 보존 기간·최대 크기·접근 권한을 설정합니다.
   - 서비스 영향 시간에 `pm2 update`를 실행하고 프로세스 복구, 자동 시작, 로그 지속성을 확인합니다.
   - `data/landing_intent.json`을 운영 데이터로 유지할지 DB·분석 시스템으로 이전할지 결정합니다.
   - SQLite WAL 파일, 로컬 에이전트 폴더, 이미지 산출물의 Git 관리 정책을 정리합니다.

3. 최종 회귀와 배포 검증을 수행합니다.
   - FastAPI 통합 테스트로 OAuth, 세션 폐기, 관리자 API, STT allowlist, 업로드 제한, 크레딧 보상을 검증합니다.
   - 프론트엔드 DOM/XSS, 로케일 키, 핵심 브라우저 사용자 흐름을 검증합니다.
   - 배포 전 `.venv/bin/python -m pytest`, TTS 집중 테스트, 로그인·OAuth·음성통화 smoke test를 실행합니다.
   - 배포 뒤 24시간 동안 오류율, TTS 실패율, 신규 가입, 성공 로그인, AI 호출량을 모니터링합니다.

4. **완료 기준**: 주요 사용자 흐름이 키보드와 다국어 환경에서 동작하고, 개인정보가 로그에 평문으로 남지 않으며, 배포 후 24시간 동안 신규 심각 오류가 없습니다.
